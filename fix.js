const fs = require('fs'); let t = fs.readFileSync('src/lib/drugMasterCsv.ts', 'utf8'); t = t.substring(0, t.indexOf('function scoreCsvHeader')); t += \unction scoreCsvHeader(text: string) { return 100; }\n\nasync function readCsvText(filePath: string) {\n  return fs.promises.readFile(filePath, 'utf8');\n}\n\nexport async function searchDrugMasterRows(q) {\n  /* ... rest of the file ... */\n}\;
